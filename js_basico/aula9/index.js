const nome = 'pedro';
const nome1 = "pedro";
const nome2 = `pedro`;
const num = 10
const num1 = 10.52
let nomeAluno;//underfined -> não aponta pra local nenhuma na mémoria
let sobrenomeAluno = null;//Nulo -> não aponta pra local nenhuma na mémoria
const boolean = true;// true ou false -> logico
// typeof nomeVariavel -> aparece o tipo


console.log(nome)